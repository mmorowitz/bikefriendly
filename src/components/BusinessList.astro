---
// BusinessList component - Interactive business directory island (Phase 2)
// Will be hydrated with client:visible in Phase 2
---

<div class="business-list-container">
  <div class="business-list-placeholder">
    <div class="business-list-controls">
      <div class="control-placeholder"></div>
      <div class="control-placeholder"></div>
      <div class="control-placeholder"></div>
      <div class="control-placeholder"></div>
    </div>
    <p>Business list loading...</p>
  </div>
</div>

<style>
  .business-list-container {
    margin-bottom: var(--space-8, 2rem);
  }

  .business-list-placeholder {
    background: var(--light-bg, #f8fafc);
    border: 2px dashed var(--border-color, #cbd5e1);
    border-radius: var(--radius-md, 0.5rem);
    padding: var(--space-8, 2rem);
    text-align: center;
    color: var(--muted-text, #64748b);
    font-size: var(--text-lg, 1.125rem);
    min-height: 200px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-4, 1rem);
  }

  .business-list-controls {
    display: flex;
    gap: var(--space-2, 0.5rem);
    margin-bottom: var(--space-4, 1rem);
  }

  .control-placeholder {
    width: 60px;
    height: 32px;
    background: var(--border-color, #cbd5e1);
    border-radius: var(--radius, 0.375rem);
    opacity: 0.6;
  }
</style>